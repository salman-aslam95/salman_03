//import React from 'react';

function Student(props) {
    return(
        <p>The student: "{props.name}" son of "{props.f_name}" has passed the anual exam by getting marks: {props.num}</p>
    )
};

export default Student;